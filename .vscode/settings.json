{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "AssestDB",
            "database": "${workspaceFolder:asset-management-cap}/db.sqlite"
        }
    ]
}